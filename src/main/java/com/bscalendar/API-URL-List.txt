API Url List

-----<< /api/member/** URL List >>-----

● 회원가입
  URL   : /api/member/join
  method: POST
  body  : mem_name, mem_position, mem_depart, mem_id, mem_pwd
  success Return: result : "ok"
     fail Retrun: result : "no"

● 회원가입시 아이디 중복 체크
  URL   : /api/member/ajaxIdChk
  method: POST
  body  : "CheckId":"mem_id"
  success Return: result : "success"
     fail Return: 401 Response
     
● 멤버 조회 Token 이용
  URL   : /api/member/getMember
  method: GET
  header: 'Authorization':'Bearer Token'
  success Return: result : "success"
     fail Return: 401 Response
     
● 멤버 정보 업데이트
  URL   : /api/member/update
  method: POST
  header: 'Authorization':'Bearer Token'
  success Return: result : "ok"
     fail Return: 401 Response
     
● 회원 탈퇴 (미완성)
  URL   : /api/member/delete
  method: DELETE
  header: 'Authorization':'Bearer Token'
  success Return: ??
     fail Return: ??
     
● 로그인
  URL   : /api/member/login
  method: POST
  body  : mem_id, mem_pwd
  success Return: token  : "생성된 토큰"
                  result : "ok"
     fail Return: 401 Response
                  result : "fail"

-----<< /api/project/** URL List >>-----

● 프로젝트 생성
  URL   : /api/project
  method: POST
  body  : team_name, team_sdate, team_edate, mem_id
  success Return: ProjectDTO Type
     fail Return: 400 Response
     
● 로그인한 유저의 소속된 프로젝트 리스트 조회
  URL   : /api/project/{mem_id}
  method: GET
  success Return: ProjectDTO Type
     fail Return: 400 Response
     
● 프로젝트에 소속된 멤버 이름을 조회
  URL   : /api/project/members/{team_idx}
  method: GET
  success Return: List<ProjectMemberDTO>
     fail Return: 400 Response
     
<<
  fail Return이 Array(0)인 것들은 NO_CONTENT 204 응답으로 바꿔야할 듯
>>
● 멤버가 소속 프로젝트 내에서 등록한 공유된 업무 리스트 조회
(수정할 필요가 있음)
  URL   : /api/project/members/work/list/{member_id}/{team_idx}
  method: GET
  success Return: List<MemberWorkDTO>
     fail Return: 204 Response
     
● 프로젝트에 멤버 초대할 때 멤버 검색 기능
  URL   : /api/project/member/read/{search}
  method: GET
  success Return: List<ProjectMemberDTO>
     fail Return: 204 Response
     
● 프로젝트에 초대가 가능한 멤버 리스트 조회
  이미 소속되어 있는 멤버는 조회되지 않음
  URL   : /api/project/member/read-all/{team_idx}
  method: GET
  success Return: List<ProjectMemberDTO>
     fail Return: 204 Response
     
● 멤버를 추가하는 기능
  URL   : /api/project/member/add/{team_idx}
  method: POST
  body  : JSON Array (mem_id)
  success Return: successMessage : ~명 투입되었습니다.
     fail Return: errorMessage : 이미 등록되어 있는 멤버입니다.
     
● 프로젝트 정보 조회
  URL   : /api/project/setting/{team_idx}
  method: GET
  success Return: ProjectDTO
     fail Return: 204 response
     
● 프로젝트 정보 수정
  URL   : /api/project/setting/{team_idx}/{team_edate}/{team_name}
  method: PATCH
  success Return: message : 프로젝트 설정이 정상적으로 변경되었습니다.
     fail Return: message : 예기치 않은 오류로 인해 설정 변경에 실패하였습니다. 다시 시도해주세요.
     
● 프로젝트 종료 처리
  URL   : /api/project/endProject/{team_idx}
  method: DELETE
  success Return: message : 프로젝트가 정상적으로 종료되었습니다.
     fail Return: message : 예기치 않은 오류로 인해 프로젝트를 종료시키지 못했습니다. 다시 시도해주세요.
     
● 프로젝트에서 멤버 내보내기
  URL   : /api/project/member/del/{team_idx}/{mem_id}
  method: DELETE
  success Return: message : mem_name 님이 정상적으로 프로젝트에서 제외 되었습니다.
     fail Return: 400 Response
     
-----<< /api/work/** URL List >>-----

● 업무 등록
  URL   : /api/work/insertWork
  method: POST
  body  : team_idx, mem_id,
          works_comment,
          works_hide,
          works_arlam,
          works_arlam_date,
          works_sdate,
          works_edate
  success Return: WorkDTO
     fail Return: 400 Response
     
● 달력 날짜에 해당하는 업무 리스트
  URL   : /api/work/list/date/{date}/{team_idx}
  method: GET
  success Return: List<WorkDTO>
     fail Return: Array(0)
     
● 업무 완료 처리 (미완성)
  URL   : /api/work/update/{works_idx}
  method: PATCH
  success Return: work : WorkDTO
     fail Return: 400 Response
                  message : 이미 완료된 업무입니다.
                  message : 업무를 완료 처리하지 못했습니다.
                  
-----<< /api/reply/** URL List >>-----

● 댓글 등록
  URL   : /api/reply
  method: POST
  body  : works_idx, reply_comment, mem_id, reply_idx
  success Return: ReplyResponseDTO
     fail Return: 등록되지 않음, 500 Response
  
● 댓글 목록 조회
  URL   : /api/reply/list?works_idx={workd_idx}
  method: GET
  success Return: List<ReplyResponseDTO>
     fail Return: Array(0)
     
● 댓글 수정
  URL   : /api/list/{reply_idx}
  method: PUT
  body  : reply_idx, reply_comment
  success Return: ReplyResponseDTO
     fail Return: 403 Response
     
● 댓글 삭제
  URL   : /api/list/{reply_idx}
  method: DELETE
  header: 'Authorization':'Bearer token'
  success Return: 200 Response
     fail Return: 403 Response 
  
  
  
  
  
  
  
  
  